let str = "2 4 2 4";
let digits = str.split(" ");

let a = digits[0];
let b = digits[1];
let c = digits[2];
let d = digits[3];

if ((a === b && c === d) || (a === c && b === d) || (a === d && b === c)) {
  console.log("YES");
} else {
  console.log("NO");
}
