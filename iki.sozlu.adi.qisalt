function TwoWordName(fullName) {
  console.log(
    fullName
      .split(" ")
      .map((name) => name.at(0).toUpperCase())
      .join(".")
  );
}
TwoWordName("Sam Harris"); // S.H
TwoWordName("patrick feeney");  // P.F
